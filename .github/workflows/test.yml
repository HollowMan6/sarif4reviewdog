name: Kubescape scanning for misconfigurations
on: [pull_request]
jobs:
  kubescape-scan:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
    - uses: actions/checkout@v3
    - uses: ./
      with:
        file: 'results.sarif'
        level: warning
